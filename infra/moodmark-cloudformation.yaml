AWSTemplateFormatVersion: '2010-09-09'
Description: MoodMark - S3, Lambda, API Gateway (MVP)

# --- Deployment Instructions ---
# 1. The stack will create the Lambda deployment S3 bucket (LambdaCodeBucket) if it does not exist.
# 2. After stack creation starts, upload your Lambda deployment packages to the bucket:
#      aws s3 cp path/to/log_activity.zip s3://<your-lambda-code-bucket>/log_activity.zip
#      aws s3 cp path/to/get_suggestion.zip s3://<your-lambda-code-bucket>/get_suggestion.zip
#    (Replace <your-lambda-code-bucket> with the value you use for LambdaCodeBucket)
# 3. If the stack fails on first run due to missing Lambda code, re-run the stack update after uploading the .zip files.
# 4. Ensure all parameter values in infra/moodmark-parameters.json are correct for your environment.
# ------------------------------

Parameters:
  S3BucketName:
    Type: String
    Default: moodmark-user-logs
    Description: S3 bucket for storing user activity logs

  LambdaCodeBucket:
    Type: String
    Description: S3 bucket containing Lambda deployment packages

  LogActivityLambdaFunctionName:
    Type: String
    Default: log-activity
    Description: Name for the Log Activity Lambda function

  GetSuggestionLambdaFunctionName:
    Type: String
    Default: get-suggestion
    Description: Name for the Get Suggestion Lambda function

  LogActivityZipKey:
    Type: String
    Default: log_activity.zip
    Description: S3 key for Log Activity Lambda deployment package

  GetSuggestionZipKey:
    Type: String
    Default: get_suggestion.zip
    Description: S3 key for Get Suggestion Lambda deployment package

  ApiGatewayName:
    Type: String
    Default: MoodMarkAPI
    Description: Name for the API Gateway

  GoogleAiApiKey:
    Type: String
    Description: Google AI API Key for Lambda (if needed)

Resources:
  MoodMarkS3Bucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref S3BucketName

  LogActivityLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: S3WritePolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:PutObject
                Resource: !Sub arn:aws:s3:::${S3BucketName}/*

  LogActivityLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref LogActivityLambdaFunctionName
      Handler: log_activity.lambda_handler
      Role: !GetAtt LogActivityLambdaRole.Arn
      Runtime: python3.9
      Timeout: 10
      Environment:
        Variables:
          S3_BUCKET: !Ref S3BucketName
      Code:
        S3Bucket: !Ref LambdaCodeBucket
        S3Key: !Ref LogActivityZipKey

  GetSuggestionLambdaRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies:
        - PolicyName: S3ReadPolicy
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:ListBucket
                Resource:
                  - !Sub arn:aws:s3:::${S3BucketName}
                  - !Sub arn:aws:s3:::${S3BucketName}/*

  GetSuggestionLambda:
    Type: AWS::Lambda::Function
    Properties:
      FunctionName: !Ref GetSuggestionLambdaFunctionName
      Handler: get_suggestion.lambda_handler
      Role: !GetAtt GetSuggestionLambdaRole.Arn
      Runtime: python3.9
      Timeout: 15
      Environment:
        Variables:
          S3_BUCKET: !Ref S3BucketName
          GOOGLE_AI_API_KEY: !Ref GoogleAiApiKey
      Code:
        S3Bucket: !Ref LambdaCodeBucket
        S3Key: !Ref GetSuggestionZipKey

  LogActivityApi:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: !Ref ApiGatewayName

  LogActivityResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref LogActivityApi
      ParentId: !GetAtt LogActivityApi.RootResourceId
      PathPart: log-activity

  LogActivityMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref LogActivityApi
      ResourceId: !Ref LogActivityResource
      HttpMethod: POST
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations
          - { LambdaArn: !GetAtt LogActivityLambda.Arn }

  GetSuggestionResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref LogActivityApi
      ParentId: !GetAtt LogActivityApi.RootResourceId
      PathPart: get-suggestion

  GetSuggestionMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref LogActivityApi
      ResourceId: !Ref GetSuggestionResource
      HttpMethod: GET
      AuthorizationType: NONE
      Integration:
        Type: AWS_PROXY
        IntegrationHttpMethod: POST
        Uri: !Sub
          - arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${LambdaArn}/invocations
          - { LambdaArn: !GetAtt GetSuggestionLambda.Arn }

  LambdaInvokePermissionLog:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt LogActivityLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com

  LambdaInvokePermissionSuggest:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !GetAtt GetSuggestionLambda.Arn
      Action: lambda:InvokeFunction
      Principal: apigateway.amazonaws.com

Outputs:
  ApiUrl:
    Description: "Base URL for MoodMark API"
    Value: !Sub "https://${LogActivityApi}.execute-api.${AWS::Region}.amazonaws.com/prod"
  S3Bucket:
    Description: "S3 bucket for activity logs"
    Value: !Ref S3BucketName
